# DATABASE_URL=mysql://root:password@mysql:3306/test-cloud-db
DATABASE_URL=mysql://root:password@localhost:3312/test-cloud-db
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=test-cloud-db
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_HOST=localhost
JWT_SECRET=seu_secret_super_seguro_aqui

# Base usada para montar a URL pública dos arquivos enviados (BucketLocalService).
# Se não definida, o backend usa o fallback http://localhost:3001
BASE_URL_FILES=http://localhost:3001

# ---------------------------------------------------------------------------
# Credenciais de usuários de teste (login no frontend)
# Obs.: são credenciais "de uso no frontend" (não são consumidas pelo backend via env).
# ---------------------------------------------------------------------------

# Usuários seed (Vituax) - sem idBrandMaster
# admin@vituax.com / Admin@123
# manager@vituax.com / Manager@123
# member@vituax.com / Member@123

# Usuários (MSP Demo Alpha) - com idBrandMaster
# admin2@mspdemoalpha.com / Admin@123456
# gerente@mspdemoalpha.com / Manager@123456
# membro@mspdemoalpha.com / Member@123456
